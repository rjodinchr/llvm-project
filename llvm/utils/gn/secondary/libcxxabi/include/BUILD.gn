import("//build_overrides/llvm.gni")

import("${llvm_root_dir}clang/resource_dir.gni")

copy("include") {
  sources = [
    "__cxxabi_config.h",
    "cxxabi.h",
  ]
  outputs = [ "$root_build_dir/include/c++/v1/{{source_target_relative}}" ]
}
