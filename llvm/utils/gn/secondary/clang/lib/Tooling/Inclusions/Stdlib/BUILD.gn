import("//build_overrides/llvm.gni")

llvm_static_library("Stdlib") {
  output_name = "clangToolingInclusionsStdlib"
  configs += [ "${llvm_root_dir}llvm/utils/gn/build:clang_code" ]
  deps = [ "${llvm_root_dir}clang/lib/AST" ]
  sources = [ "StandardLibrary.cpp" ]
}
