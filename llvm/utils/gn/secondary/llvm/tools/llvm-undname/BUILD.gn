import("//build_overrides/llvm.gni")

llvm_executable("llvm-undname") {
  deps = [
    "${llvm_root_dir}llvm/lib/Demangle",
    "${llvm_root_dir}llvm/lib/Support",
  ]
  sources = [ "llvm-undname.cpp" ]
}
