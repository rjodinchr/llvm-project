import("//build_overrides/llvm.gni")

llvm_executable("llvm-bcanalyzer") {
  deps = [
    "${llvm_root_dir}llvm/lib/Bitcode/Reader",
    "${llvm_root_dir}llvm/lib/Support",
  ]
  sources = [ "llvm-bcanalyzer.cpp" ]
}
