import("//build_overrides/llvm.gni")

llvm_executable("FileCheck") {
  deps = [
    "${llvm_root_dir}llvm/lib/FileCheck",
    "${llvm_root_dir}llvm/lib/Support",
  ]
  sources = [ "FileCheck.cpp" ]
}
