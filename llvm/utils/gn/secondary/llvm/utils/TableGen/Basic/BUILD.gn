import("//build_overrides/llvm.gni")

llvm_static_library("Basic") {
  deps = [
    "${llvm_root_dir}llvm/lib/Support",
    "${llvm_root_dir}llvm/lib/TableGen",
  ]
  sources = [
    "CodeGenIntrinsics.cpp",
    "SDNodeProperties.cpp",
  ]
}
