import("//build_overrides/llvm.gni")

import("${llvm_root_dir}llvm/utils/TableGen/tablegen.gni")

tablegen("RISCVGenAsmMatcher") {
  visibility = [
    ":AsmParser",
    "${llvm_root_dir}llvm/lib/Target/RISCV:LLVMRISCVCodeGen",
  ]
  args = [ "-gen-asm-matcher" ]
  td_file = "../RISCV.td"
}

llvm_static_library("AsmParser") {
  output_name = "LLVMRISCVAsmParser"
  deps = [
    ":RISCVGenAsmMatcher",
    "${llvm_root_dir}llvm/lib/MC",
    "${llvm_root_dir}llvm/lib/MC/MCParser",
    "${llvm_root_dir}llvm/lib/Support",
    "${llvm_root_dir}llvm/lib/Target/RISCV:RISCVGenCompressInstEmitter",
    "${llvm_root_dir}llvm/lib/Target/RISCV/MCTargetDesc",
    "${llvm_root_dir}llvm/lib/TargetParser",
  ]
  include_dirs = [ ".." ]
  sources = [ "RISCVAsmParser.cpp" ]
}
