import("//build_overrides/llvm.gni")

import("${llvm_root_dir}llvm/utils/TableGen/tablegen.gni")

tablegen("HexagonGenDisassemblerTables") {
  visibility = [ ":Disassembler" ]
  args = [ "-gen-disassembler" ]
  td_file = "../Hexagon.td"
}

llvm_static_library("Disassembler") {
  output_name = "LLVMHexagonDisassembler"
  deps = [
    ":HexagonGenDisassemblerTables",
    "${llvm_root_dir}llvm/lib/MC/MCDisassembler",
    "${llvm_root_dir}llvm/lib/Support",
    "${llvm_root_dir}llvm/lib/Target/Hexagon/MCTargetDesc",
    "${llvm_root_dir}llvm/lib/Target/Hexagon/TargetInfo",
  ]
  include_dirs = [ ".." ]
  sources = [ "HexagonDisassembler.cpp" ]
}
