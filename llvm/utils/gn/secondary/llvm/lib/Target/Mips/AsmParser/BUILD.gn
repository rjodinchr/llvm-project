import("//build_overrides/llvm.gni")

import("${llvm_root_dir}llvm/utils/TableGen/tablegen.gni")

tablegen("MipsGenAsmMatcher") {
  visibility = [ ":AsmParser" ]
  args = [ "-gen-asm-matcher" ]
  td_file = "../Mips.td"
}

llvm_static_library("AsmParser") {
  output_name = "LLVMMipsAsmParser"
  deps = [
    ":MipsGenAsmMatcher",
    "${llvm_root_dir}llvm/lib/MC",
    "${llvm_root_dir}llvm/lib/MC/MCParser",
    "${llvm_root_dir}llvm/lib/Support",
    "${llvm_root_dir}llvm/lib/Target/Mips/MCTargetDesc",
    "${llvm_root_dir}llvm/lib/Target/Mips/TargetInfo",
    "${llvm_root_dir}llvm/lib/TargetParser",
  ]
  include_dirs = [ ".." ]
  sources = [ "MipsAsmParser.cpp" ]
}
