import("//build_overrides/llvm.gni")

llvm_static_library("Support") {
  output_name = "LLVMTestingSupport"
  deps = [
    "${llvm_root_dir}llvm/lib/Support",
    "${llvm_root_dir}third-party/unittest:gtest",
  ]
  sources = [
    "Error.cpp",
    "SupportHelpers.cpp",
  ]
  testonly = true
}
