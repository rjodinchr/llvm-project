import("//build_overrides/llvm.gni")

llvm_static_library("Annotations") {
  output_name = "LLVMTestingAnnotations"
  deps = [ "${llvm_root_dir}llvm/lib/Support" ]
  sources = [ "Annotations.cpp" ]
  testonly = true
}
