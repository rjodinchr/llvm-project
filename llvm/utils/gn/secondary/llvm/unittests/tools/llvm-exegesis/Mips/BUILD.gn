import("//build_overrides/llvm.gni")

import("${llvm_root_dir}third-party/unittest/unittest.gni")

unittest("LLVMExegesisMipsTests") {
  deps = [
    "${llvm_root_dir}llvm/lib/DebugInfo/Symbolize",
    "${llvm_root_dir}llvm/lib/MC",
    "${llvm_root_dir}llvm/lib/MC/MCParser",
    "${llvm_root_dir}llvm/lib/Object",
    "${llvm_root_dir}llvm/lib/Support",
    "${llvm_root_dir}llvm/lib/Target/Mips",

    # Exegesis reaches inside the Target/Mips tablegen internals and must
    # depend on these Target/Mips-internal build targets.
    "${llvm_root_dir}llvm/lib/Target/Mips/MCTargetDesc",
    "${llvm_root_dir}llvm/tools/llvm-exegesis/lib",
    "${llvm_root_dir}llvm/tools/llvm-exegesis/lib/Mips",
  ]
  include_dirs = [
    "${llvm_root_dir}llvm/lib/Target/Mips",
    "${llvm_root_dir}llvm/tools/llvm-exegesis/lib",
  ]
  sources = [
    "BenchmarkResultTest.cpp",
    "RegisterAliasingTest.cpp",
    "SnippetGeneratorTest.cpp",
    "TargetTest.cpp",
  ]
}
