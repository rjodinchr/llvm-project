import("//build_overrides/llvm.gni")

import("${llvm_root_dir}third-party/unittest/unittest.gni")

unittest("LLVMExegesisAArch64Tests") {
  deps = [
    "${llvm_root_dir}llvm/lib/DebugInfo/Symbolize",
    "${llvm_root_dir}llvm/lib/MC",
    "${llvm_root_dir}llvm/lib/MC/MCParser",
    "${llvm_root_dir}llvm/lib/Object",
    "${llvm_root_dir}llvm/lib/Support",
    "${llvm_root_dir}llvm/lib/Target/AArch64",

    # Exegesis reaches inside the Target/AArch64 tablegen internals and must
    # depend on this Target/AArch64-internal build target.
    "${llvm_root_dir}llvm/lib/Target/AArch64/MCTargetDesc",
    "${llvm_root_dir}llvm/tools/llvm-exegesis/lib",
    "${llvm_root_dir}llvm/tools/llvm-exegesis/lib/AArch64",
  ]
  include_dirs = [
    "${llvm_root_dir}llvm/lib/Target/AArch64",
    "${llvm_root_dir}llvm/tools/llvm-exegesis/lib",
  ]
  sources = [ "TargetTest.cpp" ]
}
