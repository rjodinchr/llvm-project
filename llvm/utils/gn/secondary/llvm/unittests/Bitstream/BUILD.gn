import("//build_overrides/llvm.gni")

import("${llvm_root_dir}third-party/unittest/unittest.gni")

unittest("BitstreamTests") {
  deps = [ "${llvm_root_dir}llvm/lib/Bitstream/Reader" ]
  sources = [
    "BitstreamReaderTest.cpp",
    "BitstreamWriterTest.cpp",
  ]
}
