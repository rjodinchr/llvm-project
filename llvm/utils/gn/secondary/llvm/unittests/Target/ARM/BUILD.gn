import("//build_overrides/llvm.gni")

import("${llvm_root_dir}third-party/unittest/unittest.gni")

unittest("ARMTests") {
  deps = [
    "${llvm_root_dir}llvm/lib/CodeGen/MIRParser",
    "${llvm_root_dir}llvm/lib/MC",
    "${llvm_root_dir}llvm/lib/Support",
    "${llvm_root_dir}llvm/lib/Target",
    "${llvm_root_dir}llvm/lib/Target/ARM:LLVMARMCodeGen",
    "${llvm_root_dir}llvm/lib/Target/ARM/MCTargetDesc",
    "${llvm_root_dir}llvm/lib/Target/ARM/TargetInfo",
    "${llvm_root_dir}llvm/lib/Target/ARM/Utils",
    "${llvm_root_dir}llvm/lib/TargetParser",
  ]
  include_dirs = [ "${llvm_root_dir}llvm/lib/Target/ARM" ]
  sources = [
    "InstSizes.cpp",
    "MachineInstrTest.cpp",
  ]
}
