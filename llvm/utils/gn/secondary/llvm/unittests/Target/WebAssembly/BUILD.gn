import("//build_overrides/llvm.gni")

import("${llvm_root_dir}third-party/unittest/unittest.gni")

unittest("WebAssemblyTests") {
  deps = [
    "${llvm_root_dir}llvm/lib/CodeGen",
    "${llvm_root_dir}llvm/lib/CodeGen/MIRParser",
    "${llvm_root_dir}llvm/lib/IR",
    "${llvm_root_dir}llvm/lib/MC",
    "${llvm_root_dir}llvm/lib/Support",
    "${llvm_root_dir}llvm/lib/Target/WebAssembly:LLVMWebAssemblyCodeGen",
    "${llvm_root_dir}llvm/lib/Target/WebAssembly/MCTargetDesc",
    "${llvm_root_dir}llvm/lib/Target/WebAssembly/TargetInfo",
    "${llvm_root_dir}llvm/lib/TargetParser",
  ]
  include_dirs = [ "${llvm_root_dir}llvm/lib/Target/WebAssembly" ]
  sources = [ "WebAssemblyExceptionInfoTest.cpp" ]
}
