import("//build_overrides/llvm.gni")

import("${llvm_root_dir}third-party/unittest/unittest.gni")

unittest("X86Tests") {
  deps = [
    "${llvm_root_dir}llvm/lib/Analysis",
    "${llvm_root_dir}llvm/lib/AsmParser",
    "${llvm_root_dir}llvm/lib/CodeGen",
    "${llvm_root_dir}llvm/lib/CodeGen/MIRParser",
    "${llvm_root_dir}llvm/lib/IR",
    "${llvm_root_dir}llvm/lib/MC",
    "${llvm_root_dir}llvm/lib/Passes",
    "${llvm_root_dir}llvm/lib/Support",
    "${llvm_root_dir}llvm/lib/Target",
    "${llvm_root_dir}llvm/lib/Target/X86:LLVMX86CodeGen",
    "${llvm_root_dir}llvm/lib/Target/X86/MCTargetDesc",
    "${llvm_root_dir}llvm/lib/Target/X86/TargetInfo",
    "${llvm_root_dir}llvm/lib/TargetParser",
    "${llvm_root_dir}llvm/lib/Transforms/InstCombine",
  ]
  include_dirs = [ "${llvm_root_dir}llvm/lib/Target/X86" ]
  sources = [
    "MachineSizeOptsTest.cpp",
    "TernlogTest.cpp",
  ]
}
