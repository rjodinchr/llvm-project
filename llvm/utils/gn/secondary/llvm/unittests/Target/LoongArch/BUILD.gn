import("//build_overrides/llvm.gni")

import("${llvm_root_dir}third-party/unittest/unittest.gni")

unittest("LoongArchTests") {
  deps = [
    "${llvm_root_dir}llvm/lib/CodeGen/MIRParser",
    "${llvm_root_dir}llvm/lib/Support",
    "${llvm_root_dir}llvm/lib/Target",
    "${llvm_root_dir}llvm/lib/Target/LoongArch:LLVMLoongArchCodeGen",
    "${llvm_root_dir}llvm/lib/Target/LoongArch/MCTargetDesc",
    "${llvm_root_dir}llvm/lib/Target/LoongArch/TargetInfo",
  ]
  include_dirs = [ "${llvm_root_dir}llvm/lib/Target/LoongArch" ]
  sources = [
    "InstSizes.cpp",
    "MCInstrAnalysisTest.cpp",
  ]
}
