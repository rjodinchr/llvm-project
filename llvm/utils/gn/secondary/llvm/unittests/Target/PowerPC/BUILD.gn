import("//build_overrides/llvm.gni")

import("${llvm_root_dir}third-party/unittest/unittest.gni")

unittest("PowerPCTests") {
  deps = [
    "${llvm_root_dir}llvm/lib/Support",
    "${llvm_root_dir}llvm/lib/Target",
    "${llvm_root_dir}llvm/lib/Target/PowerPC:LLVMPowerPCCodeGen",
    "${llvm_root_dir}llvm/lib/Target/PowerPC/MCTargetDesc",
    "${llvm_root_dir}llvm/lib/Target/PowerPC/TargetInfo",
    "${llvm_root_dir}llvm/lib/TargetParser",
  ]
  sources = [ "AIXRelocModelTest.cpp" ]
}
