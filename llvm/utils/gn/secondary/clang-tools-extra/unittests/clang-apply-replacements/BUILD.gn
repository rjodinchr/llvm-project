import("//build_overrides/llvm.gni")

import("${llvm_root_dir}third-party/unittest/unittest.gni")

unittest("ClangApplyReplacementsTests") {
  configs += [ "${llvm_root_dir}llvm/utils/gn/build:clang_code" ]
  deps = [
    "${llvm_root_dir}clang-tools-extra/clang-apply-replacements",
    "${llvm_root_dir}clang/lib/Basic",
    "${llvm_root_dir}clang/lib/Tooling/Core",
    "${llvm_root_dir}clang/lib/Tooling/Refactoring",
  ]
  include_dirs = [ "${llvm_root_dir}clang-tools-extra/clang-apply-replacements/include" ]
  sources = [ "ApplyReplacementsTest.cpp" ]
}
