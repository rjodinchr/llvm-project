import("//build_overrides/llvm.gni")

llvm_source_set("remote") {
  configs += [ "${llvm_root_dir}llvm/utils/gn/build:clang_code" ]
  deps = [
    "${llvm_root_dir}clang-tools-extra/clangd/support",
    "${llvm_root_dir}llvm/lib/Support",
  ]
  include_dirs = [ "../.." ]
  sources = []
  if (false) {
    sources += [ "Client.cpp" ]
  }
}
