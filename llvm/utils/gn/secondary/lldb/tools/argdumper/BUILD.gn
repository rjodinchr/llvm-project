import("//build_overrides/llvm.gni")

llvm_executable("lldb-argdumper") {
  configs += [ "${llvm_root_dir}llvm/utils/gn/build:lldb_code" ]
  deps = [ "${llvm_root_dir}llvm/lib/Support" ]
  sources = [ "argdumper.cpp" ]

  # XXX ADD_TO_FRAMEWORK
}
